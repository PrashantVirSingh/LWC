====childCmp.html====
<template>
    
    <div>Explaination of LWC Day 15 </div>
</template>

  ====childCmp.js====
import { LightningElement } from 'lwc';

export default class ChildCmp extends LightningElement {

    state;

    constructor(){
        super();
        this.state=this.template.isConnected;
        console.log('@@@ what is the state in constructor? : ',this.state);
    }
    connectedCallback(){

        console.log('@@@ ChildCmp connectedCallback: ',this.template.isConnected);
        this.state=this.template.isConnected;
        console.log('@@@ what is the state? : ',this.state);
    }
    
}

=================Explanation=============
https://youtu.be/8pkrxeDTLd0?si=nUXBFRJxd3SozrUX

============Interview QA=================
1. Can we set or access properties in connectedCallback()?
Ans: Yes

<template>
    
 {text}
</template>

import { LightningElement } from 'lwc';
export default class ChildCmp extends LightningElement {

   
  text;

  connectedCallback(){
    this.text='12';
  }
   
    }
          

    
2. Can we access elements the cmp owns inside connectedCallback()?
Ans: No
<template>
    
   <div> <lightning-input type="checkbox" label="Red" checked> </lightning-input></div>
</template>

import { LightningElement } from 'lwc';

export default class ChildCmp extends LightningElement {

   ele;
   
    connectedCallback(){
        this.ele= this.template.querySelector('lightning-input').value;
        console.log('@@@ element '+this.ele);
          
          
    }
    
}
Error:  

3. Can we call apex method inside connectedCallback()?
Ans: Yes
public with sharing class Fetch_Account {
    @AuraEnabled(cacheable=true)
      public static list<Account> fetchAcc(){
          list<Account> lstAcc = new list<Account>();
          lstAcc=[select Id, Name, Active__c,NumberOfEmployees,Website,Phone from Account Where Name Like 'W%' Limit 1];
          return lstAcc;
  
      }
  }
  
  
  <template>
    
  

<template for:each={accounts} for:item="acc">
{acc.Name}

</template>
</template>

import { LightningElement } from 'lwc';
import apexM from '@salesforce/apex/Fetch_Account.fetchAcc';
export default class ChildCmp extends LightningElement {

   
   accounts = [];
    connectedCallback(){
      apexM()
            .then(result => {
                this.accounts = result;
                this.error = undefined;
            })
            .catch(error => {
                this.error = error;
                this.accounts = [];
            });
}
          
    }


4. Can we create & dispatch event in connectedCallback()?
Ans: Yes

<template>
    
  <h1>Hello Child!</h1>
</template>

import { LightningElement } from 'lwc';
export default class ChildCmp extends LightningElement {

   
  connectedCallback(){
    const evt= new CustomEvent("helloparent");
    this.dispatchEvent(evt);
  }
   
   
    }
          

	<template>
    <c-child-Cmp onhelloparent={evtlistner}></c-child-Cmp>  
    <h1>hello parent!</h1>
</template>
import { LightningElement } from 'lwc';

export default class ParentCmp extends LightningElement {
    evtlistner(){
       console.log('@@@ event listner');
    }
}

5. Can we use navigation service inside connectedCallback()?
Ans: Yes


6. Can we call uiApi from connectedCallback()?
Ans: yes
=================Explanation=============
https://www.youtube.com/watch?v=qJAK9gLmTok&list=PL3y3JMXmJvomioQWOZzxeSj8-ilUwsnPL&index=20
